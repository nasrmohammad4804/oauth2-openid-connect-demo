server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/security-daily-code-buffer
    username: root
    password: MohammadN@sr13804804
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  mail:
    host: smtp.gmail.com
    properties:
      mail:
        transport:
          protocol: smtp
        smtp:
          auth: true
          connectiontimeout: 5000
          writetimeout: 5000
          starttls:
            enable: true
            require: true
      debug: true
    port: 587
    username: mnasr13804804@gmail.com
    password: MOHAMMAD13804804
  security:
    oauth2:
      client:
        registration:
          app:
            provider: spring
            client-id: client1
            client-secret: secret1
            authorization-grant-type: authorization_code
            redirect-uri: "http://127.0.0.1:8080/login/oauth2/code/{registrationId}"

            scope: openid,read
            client-name: app
        provider:
          spring:
            issuer-uri: http://auth-server:9000

